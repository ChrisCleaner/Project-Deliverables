 fccc kp cmp add  distr general january chinese original english                                 cmp    cmp     g       cmp   cmp  cmp  cmp           cmp   cmp  cmp  cmp             cmp      ge fccc kp cmp add  cmp         cmp                                                                        fccc kp cmp  cmp    ge fccc kp cmp add                                cp                                     ge fccc kp cmp add  cmp    g             cmp            cmp                         cmp    b  cmp                    cmp                  ge fccc kp cmp add  cmp   cmp  cmp  cmp                      cmp cmp cmp cmp cmp  cmp   cmp cmp cmp cmp cmp cmp  cmp       cmp     cmp  a  b                                   cmp cmp cmp  cmp   cmp  cmp       cmp    b      cmp       cmp  cmp       cmp            cmp      cmp  ge fccc kp cmp add               cmp                               cmp  cmp   cmp     cmp                       cmp cmp  cmp                                   cmp       cmp      cmp   ge fccc kp cmp add  cmp                  cmp   cmp     cmp        cmp    b                     b       cmp  cmp         b    cmp               cmp                  ge fccc kp cmp add              cmp   ge fccc kp cmp add        cmp      cmp                    cmp       b       cmp         b       b                                               cmp    cp   ge fccc kp cmp add         b   cmp     cmp     cmp           cmp        b                 aau     cmp            cmp              cmp          cmp          cmp       cmp     ge fccc kp cmp add c       cmp                cmp      cmp       d         cmp   aau   cmp    cmp   cmp     eru  cer   aau   cmp           aau       aau  cmp    aau  cmp     aau  cmp   cmp    ge fccc kp cmp add  cmp   tcer  cmp   lcer  cmp   cmp      lcer  cmp   cmp      lcer  cmp     tcer  aau cer eru rmu   tcer  cmp   lcer  aau cer eru   rmu  cmp    lcer  aau cer eru rmu   lcer  cmp     lcer  aau cer eru rmu   lcer e    cmp    cmp   f    cmp    g    cmp     cer  aau  eru       aau  aau   eru   cer  ge fccc kp cmp add  cmp        aau      a    cmp     aau     aau   cmp    aau     cmp   cmp    cmp     cmp   tcer    cmp   lcer    cmp   cmp        lcer   cmp   cmp        lcer   cmp   tcer   aau cer eru rmu   tcer  ge fccc kp cmp add  cmp  lcer   aau cer eru   rmu   cmp   lcer  aau cer eru rmu   lcer    cmp   lcer  aau cer eru rmu   lcer   b eru aau  rmu   cmp    cmp              cmp   aau      aau  c       cmp             cmp  cmp  cmp  cmp   eru cer aau  rmu        aau     aau    aau      aau  aau      cmp   cmp    eru cer aau   rmu     eru cer aau   rmu   ge fccc kp cmp add   cmp    cmp      eru cer   aau   eru cer   aau     eru cer aau   rmu   cmp  d    cmp                cmp            cmp       tcer  lcer  cmp   cer   cmp     cer   e    cmp    cmp     aau     aau    aau  ge fccc kp cmp add      cmp           cmp     cmp       cmp   cmp     cmp     cmp      cmp     cmp     rmu    cmp       aau     aau  ge fccc kp cmp add         b                                                 no            aau  eru  rmu  tcer   lcer  cer  cmp    aau eru rmu cer  tcer  lcer ge fccc kp cmp add   a                   cmp     cmp    cmp    cmp               cmp  cmp          cmp     cmp      cmp     cmp         tcer   tcer  cmp   lcer   lcer  cmp    lcer   lcer  cmp   cmp    lcer    lcer  cmp    cmp   ge fccc kp cmp add    cmp     tcer   aau  cer  eru  rmu   tcer  tcer   aau cer eru   rmu  lcer   lcer   lcer   aau cer eru rmu   lcer  lcer   lcer    aau cer eru rmu   lcer  lcer  b                    cmp              cmp      cmp    eru     aau       cmp    rmu          cmp        cmp    eru  ge fccc kp cmp add    aau       cmp   rmu         cmp  cmp   cmp    cmp        rmu       cmp   cmp  rmu      cmp   cmp  aau eru rmu   cer      cmp    tcer  tcer  aau cer eru rmu   tcer    lcer  lcer  lcer  aau cer eru rmu   rmu    lcer  lcer  aau cer eru rmu   lcer    lcer  lcer      lcer  lcer   aau cer eru rmu   lcer      lcer  lcer                 cmp       ge fccc kp cmp add    cmp                        ppsr    ppsr  aau       c                                         cmp     ccs          cmp     cmp    cmp    cmp   ge fccc kp cmp add d    ppsr     ppsr  aau  ppsr  aau    aau         aau              e   cmp      aau      cmp      aau       aau   aau    sop     aau        eru     eru   cmp      eru   sop     eru    cmp         eru       eru   eru     sop     eru     ge fccc kp cmp add f       g        cmp     cmp    tcer lcer  cer              tcer    tcer   tcer       tcer       lcer    lcer   lcer     lcer   lcer    cdm     lcer       lcer    cer             ge fccc kp cmp add                  h               i     cmp               aau     aau      cmp   aau        cmp         cmp             eru   cer      eru   cer   ppsr      ppsr  aau      ppsr  aau     ge fccc kp cmp add j                   k      ppsr    aau    aau      l      tcer lcer  ccs  cer          tcer lcer  ccs  cer      tcer  lcer            tcer lcer                  ge fccc kp cmp add      tcer lcer  ccs  cer          m                             n        cmp                  e           ge fccc kp cmp add      cmp          cmp     cp    cmp  cmp      cmp      cmp      cmp     cmp                           ge fccc kp cmp add  cmp   cmp  cmp  cmp                      cmp  cmp  cmp  cmp  cmp  cmp  cmp   cmp  cmp  cmp  cmp  cmp  cmp  cmp  cmp  cmp  cmp  cmp  cmp  cmp  cmp       cmp       a  b                                     cmp  cmp    cmp  cmp       cmp  cmp  cmp  cmp                                cmp                   ge fccc kp cmp add    cp  cmp                        cmp              cmp   cmp     cmp        cmp  cmp   cmp      cmp      cmp             cmp   cmp  cmp    cmp       cmp  cmp    cmp     d     cmp  cmp   cmp    cmp   e    cmp  e  cmp                 ge fccc kp cmp add   cmp      cmp      cmp                    cmp             cmp   cmp  cmp    cmp   cmp    cmp       b    cmp                                       cmp                   extenible markup language    ge fccc kp cmp add   cmp            cmp    ge fccc kp cmp add            cmp              cmp     cmp             cmp        b    cmp     cmp     cmp                 cmp        b                      cmp   ge fccc kp cmp add       cmp    cmp      b                 cmp             cmp      cmp      cmp     cmp        cmp      ge fccc kp cmp add                  cmp   cmp    cmp           cmp  c g    cmp   cmp    cmp               eru cer tcer lcer aau  rmu   tcer  lcer  eru cer  aau        cmp             eru cer tcer lcer aau  rmu   tcer  lcer       ge fccc kp cmp add       cmp              cp      cmp      cp                             cmp  cmp    cmp       cp          cmp       cp                     cp            ge fccc kp cmp add    cmp                   cmp   cmp  cmp     cmp          ge fccc kp cmp add            cmp      cmp                     cmp                          cmp      cmp                   ge fccc kp cmp add   cmp                   cmp            cmp                       cmp    cmp     cmp     cmp              cmp                       ge fccc kp cmp add      cmp                         cmp                                 cmp        cmp        hfc  pfc             cmp                        cmp   ge fccc kp cmp add         cmp                        cmp         cp        cmp     ge fccc kp cmp add         hfc pfc  hfc pfc         b c                      d    f  g  h         d    f  g  h  i         d    ge fccc kp cmp add       n hfc pfc  n hfc pfc         b c                      d    f  g  h         d    f  g  h  i         d    ge fccc kp cmp add          hfc pfc  hfc pfc           b                   b          b            b            b d  d        d   ge fccc kp cmp add   hfc pfc  hfc pfc d         d         d    na  na   na   na          b          b f   f         b        e  na          ge fccc kp cmp add      na   b                c            e      ge fccc kp cmp add          n hfc pfc  n hfc pfc           b                   b          b            b            b d  d        d   ge fccc kp cmp add   n hfc pfc  n hfc pfc d         d         d    na  na   na   na          b          b f   f         b        e  na          ge fccc kp cmp add      na   b                c            e     ge fccc kp cmp add        b     hfc pfc  n hfc pfc              d               f            g            cef ne g           h   cef hc l           ge fccc kp cmp add   hfc pfc  n hfc pfc   g l            l  k           k           k           k          na            l  l  l na       ge fccc kp cmp add      na    b         c                  crf  kp i a        e  crf  kp i a          f   g  crf  kp i b kp i b  kp i b    h   j   k   l     ge fccc kp cmp add      b       hfc pfc  n hfc pfc              d               f            g            cef ne g           h   cef hc l           ge fccc kp cmp add   hfc pfc  n hfc pfc   g l            l  k           k           k           k          na            l  l  l na       ge fccc kp cmp add      na    b         c   crf  kp i a          e  crf  kp i a          f                g  crf  kp i b   h    crf  kp i b    j   k   l          ge fccc kp cmp add  cmp            cmp   cmp   cmp   cmp  cmp            cmp  cmp   cmp                                                               ge fccc kp cmp add                                           a                               ge fccc kp cmp add b                               c                                      d                        cmp   cmp  cmp    ge fccc kp cmp add                    e         cmp   cmp  cmp              lulucf        lulucf       lulucf         ge